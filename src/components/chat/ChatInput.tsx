import React from 'react'

interface ChatInputProps {
  onSubmit: (message: string) => void;
  isLoading: boolean;
}
const ChatInput = () => {
  return (
    <div>ChatInput</div>
  )
}

export default ChatInput